<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Only the contact-cards subfolder is shared, never the whole cache or root. -->
    <cache-path name="contact_cards" path="contact_cards/" />
</paths>
